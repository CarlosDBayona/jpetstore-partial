<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.jpetstore.mapper.ItemMapper">

  <select id="getItemListByProduct" resultType="com.example.jpetstore.domain.Item">
    SELECT
      itemid AS itemId,
      productid AS productId,
      listprice AS listPrice,
      unitcost AS unitCost,
      supplier AS supplierId,
      status,
      attr1 AS attribute1,
      attr2 AS attribute2,
      attr3 AS attribute3,
      attr4 AS attribute4,
      attr5 AS attribute5
    FROM item
    WHERE productid = #{productId}
  </select>

</mapper>
